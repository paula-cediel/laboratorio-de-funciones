function promedio(a, b, c) {
    return (a + b + c) / 3;
}

function suma(a, b) {
    return a + b;
}

function resta(a, b) {
    return a - b;
}

// Exporta TODAS las funciones
module.exports = { promedio, suma, resta };
